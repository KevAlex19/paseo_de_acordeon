class TasktoDo {
  String image,title,body,date,type;
  double latitude;
  double longitude;
  int like;

  TasktoDo(this.image, this.title, this.body, this.date, this.type, this.latitude, this.longitude, this.like);
}